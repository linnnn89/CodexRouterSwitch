# v1.2 现代中文界面设计 QA

## 对比目标

- 设计真值：`docs/modern-ui-reference-v1.2.png`
- 实现截图：`docs/codex-router-switch-v1.2.png`
- 并排证据：`docs/design-qa-comparison-v1.2.png`
- 状态：本地路由运行正常，并显示 Codex 重启提示
- 主题：浅色

## 视口与归一化

- 设计图像素：`1278 × 1230`
- 实现截图像素：`820 × 760`
- 实现逻辑视口：`820 × 760`，Windows Forms，96 DPI（100%），
  `DrawToBitmap` 以 1:1 逻辑像素捕获
- 设计图没有 CSS 视口或 DPI 元数据；并排对比时按高度等比缩放为
  `790 × 760`，实现截图保持原始 `820 × 760`
- 实现窗口在同宽下比概念图短约 3.7%，用于兼顾常见 Windows 工作区高度；
  左右分栏比例、标题栏、信息区和固定底栏的视觉层级保持一致

## 全图对比

并排证据显示，以下核心结构与所选方案一致：

- 自绘浅蓝标题栏、应用标识和现代窗口控制；
- 左侧约三分之一宽度的连接方式选择区；
- 右侧状态标题、连接地址、四项关键事实和重启提醒；
- 底部固定系统状态与三项操作；
- 选中态使用浅蓝填充、蓝色强调条和圆形勾选；
- 所有按钮均为圆角自绘按钮，不再使用系统方形按钮。

没有发现遮挡、裁切、重叠、错误换行或核心区域比例失真。

## 必查表面

- 字体与排版：使用 `Microsoft YaHei UI`，标题、状态、事实值和辅助文字
  具有明确的四级层次；中文没有截断。设计图与实现的字重存在轻微渲染差异，
  属于概念图字体与 Windows 原生字体的预期差异。
- 间距与布局：分栏、内边距、事实网格和固定底栏与设计意图一致；
  `800 × 720` 最小窗口下，异常操作与重启提醒同时显示仍无重叠。
- 颜色与视觉令牌：浅灰蓝导航区、白色内容区、蓝色主操作、绿色健康状态、
  琥珀色警告和红色错误均保持语义一致，正文对比度充足。
- 图像与资产：设计中没有照片或插画；界面图标来自 Windows
  `Segoe MDL2 Assets` 图标库，没有使用占位图、自制 SVG 或位图拉伸。
- 文案与内容：应用自有的标题、字段、按钮、状态、错误、剪贴板步骤和诊断报告
  均为中文；仅保留 Codex、OpenAI、OpenRouter、模型名、PID、IP 地址和路径等
  品牌或技术值。
- 交互与可访问性：模式项保留单选语义，按钮支持悬停、按下、键盘焦点和禁用态；
  状态刷新期间的变更入口仍被锁定。隐藏自检未显示窗口，也未执行配置变更。

## 聚焦区域

未另做裁剪对比。并排证据宽度为 `1634 × 800`，标题、模式项、事实值、提醒文案、
按钮与图标在原始实现分辨率下均可直接辨认，额外裁剪不会提供新的判断信息。

## 对比历史

### 第 1 次对比

- 证据：`docs/design-qa-comparison-v1.2.png`
- P0：无
- P1：无
- P2：无
- 结果：核心结构、状态层级、中文文案、圆角控件和固定底栏均达到所选方案要求，
  不需要阻断性交互或视觉修正。

## 后续微调

- P3：Windows 图标库中的盾牌、路由设备和信息图标与概念图的笔画粗细略有差异。
- P3：`DrawToBitmap` 不包含 Windows 11 的 DWM 外层圆角与窗口阴影；真实窗口会请求
  Windows 11 圆角属性，Windows 10 则保持方形外框。

## 实现检查清单

- [x] 纯中文主界面
- [x] 现代圆角按钮和模式项
- [x] 左右分栏与固定底栏
- [x] 正常、原生、降级、未跟踪、错误、处理中和重启提醒状态
- [x] 最小窗口下无裁切
- [x] 无配置变更的界面捕获与自检

final result: passed
